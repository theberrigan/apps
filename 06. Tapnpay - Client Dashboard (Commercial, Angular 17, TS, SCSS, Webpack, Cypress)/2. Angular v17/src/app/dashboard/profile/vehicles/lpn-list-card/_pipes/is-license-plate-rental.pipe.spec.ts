import { IsLicensePlateRentalPipe } from './is-license-plate-rental.pipe';

describe('IsLicensePlateRentalPipe', () => {
  it('create an instance', () => {
    const pipe = new IsLicensePlateRentalPipe();
    expect(pipe).toBeTruthy();
  });
});
