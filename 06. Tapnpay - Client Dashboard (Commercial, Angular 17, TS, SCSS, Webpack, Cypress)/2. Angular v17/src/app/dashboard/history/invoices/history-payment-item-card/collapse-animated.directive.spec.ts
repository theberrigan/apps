import { CollapseAnimatedDirective } from './collapse-animated.directive';

describe('CollapseAnimatedDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapseAnimatedDirective();
    expect(directive).toBeTruthy();
  });
});
