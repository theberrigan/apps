import { TaNameToShowPipe } from './ta-name-to-show.pipe';

describe('TaNameToShowPipe', () => {
  it('create an instance', () => {
    const pipe = new TaNameToShowPipe();
    expect(pipe).toBeTruthy();
  });
});
